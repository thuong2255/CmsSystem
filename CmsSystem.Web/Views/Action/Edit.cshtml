@model CmsSystem.Web.Models.ActionVm

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="content">
    <div class="row">
        <!-- left column -->
        <div class="col-md-12">
            <!-- general form elements -->
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">Sửa menu</h3>
                </div><!-- /.box-header -->
                @using (Html.BeginForm("Edit", "Action", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()

                    @Html.HiddenFor(m => m.Id)
                    @Html.HiddenFor(m => m.CreatedDate)
                    @Html.HiddenFor(m => m.CreatedBy)

                    <div class="box-body">
                        <div class="form-group">
                            @Html.LabelFor(m => m.Position)
                            @Html.TextBoxFor(m => m.Position, new { @class = "form-control" })
                        </div>
                        <div class="form-group">
                            @Html.LabelFor(m => m.Name)
                            @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                        </div>
                        <div class="form-group">
                            @Html.LabelFor(m => m.Link)
                            @Html.TextBoxFor(m => m.Link, new { @class = "form-control" })
                        </div>
                        <div class="checkbox">
                            @Html.CheckBoxFor(m => m.Display, new { @checked = "checked", @style = "margin-left:0px" }) <label>@Html.LabelFor(m => m.Display)</label>
                        </div>
                        <div class="form-group">
                            @Html.LabelFor(m => m.ParentId)
                            @Html.DropDownList("ParentId", null, "Danh mục gốc", new { @class = "form-control" })
                        </div>
                    </div><!-- /.box-body -->

                    <div class="box-footer">
                        <div class="pull-left">
                            @Html.ActionLink("Quay lại", "Index", null, new { @class = "btn btn-success" })
                        </div>
                        <div class="pull-right">
                            <button type="submit" class="btn btn-primary">Cập nhật</button>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</section>
